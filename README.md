# weather-etl-pipeline-n8n
Incremental ETL workflow built with n8n to retrieve weather data from Open-Meteo API, normalize JSON responses and load structured records into Google Sheets for BI analysis.
